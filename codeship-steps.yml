- type: serial
  service: build
  #tag: ^(master|develop)
  steps:
    - command: chmod a+x /project/gradlew
    - command: cd project
    - command: /project/gradlew build
    - command: /project/gradlew test
