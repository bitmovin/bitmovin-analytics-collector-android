- type: serial
  service: build
  #tag: ^(master|develop)
  steps:
    - command: chmod a+x /project/gradlew
    - command: cd project; /project/gradlew build
    - command: cd project; /project/gradlew test
